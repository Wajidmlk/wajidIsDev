export type tpNavState = {
  items: {
    id: string,
    placeholder: string,
    parentId: string,
  }[],
};

export type tpFooterState = {
  items: {
    id: string,
    placeholder: string,
    parentId: string,
  }[],
};
