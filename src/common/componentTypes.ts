export type tpNavState = {
  items: {
    id: string,
    paceHolder: string,
    parentId: string,
  }[],
};

export type tpFooterState = {
  items: {
    id: string,
    paceHolder: string,
    parentId: string,
  }[],
};
